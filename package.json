{
  "name": "raboard",
  "displayName": "raBoard",
  "description": "Lightweight bulletin board/chat extension backed by a shared UNC folder.",
  "version": "0.1.0",
  "publisher": "your-name",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:raBoard.openTimeline",
    "onCommand:raBoard.switchRoom",
    "onCommand:raBoard.compactLogs"
  ],
  "main": "extension/dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "raBoard.openTimeline",
        "title": "raBoard: Open Timeline"
      },
      {
        "command": "raBoard.switchRoom",
        "title": "raBoard: Switch Room"
      },
      {
        "command": "raBoard.compactLogs",
        "title": "raBoard: Compact Logs…"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "raBoard",
      "properties": {
        "raBoard.shareRoot": {
          "type": "string",
          "default": "\\\\mysv01\\board",
          "description": "UNC path to the shared board root.",
          "markdownDescription": "UNC path to the shared board root (for example `\\\\mysv01\\board`)."
        },
        "raBoard.defaultRoom": {
          "type": "string",
          "default": "general",
          "description": "Room to join on startup.",
          "markdownDescription": "Logical room to join on startup (default `general`)."
        },
        "raBoard.userName": {
          "type": "string",
          "description": "Override for the user name reported in presence.",
          "markdownDescription": "Override for the user name reported in presence. Leave empty to use the OS account name as described in the requirements."
        },
        "raBoard.pollIntervalMs": {
          "type": "number",
          "default": 5000,
          "minimum": 1000,
          "description": "Polling interval for message updates in milliseconds.",
          "markdownDescription": "Polling interval for message updates in milliseconds (default 5000ms, minimum 1000ms)."
        },
        "raBoard.presenceTtlSec": {
          "type": "number",
          "default": 60,
          "minimum": 15,
          "description": "Seconds before a user is considered offline if presence is stale.",
          "markdownDescription": "Seconds before a user is considered offline if presence is stale (default 60s, minimum 15s)."
        },
        "raBoard.maxImageMB": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "description": "Maximum inline image size in megabytes before falling back to download links.",
          "markdownDescription": "Maximum inline image size in megabytes before falling back to download links (default 10MB)."
        },
        "raBoard.maxInlinePx": {
          "type": "number",
          "default": 240,
          "minimum": 1,
          "description": "Maximum rendered image height in pixels.",
          "markdownDescription": "Maximum rendered image height in pixels for inline previews (default 240px)."
        }
      }
    }
  },
  "scripts": {
    "lint": "npm run lint:eslint && npm run lint:prettier",
    "lint:eslint": "eslint --ext .ts extension/src",
    "lint:prettier": "prettier --check \"extension/src/**/*.ts\" \"media/**/*.{css,html,js}\"",
    "compile": "tsc -p extension/tsconfig.json",
    "watch": "tsc -p extension/tsconfig.json --watch",
    "package": "npm run compile && vsce package",
    "test": "vitest",
    "test:integration": "echo \"Integration tests are not yet implemented\"",
    "prepare": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@types/vscode": "^1.85.0",
    "@vscode/test-electron": "^2.3.3",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.1.0",
    "typescript": "5.3.3",
    "vitest": "^1.0.4",
    "@vscode/vsce": "^2.16.0"
  },
  "dependencies": {
    "vscode-uri": "^3.0.7"
  }
}
